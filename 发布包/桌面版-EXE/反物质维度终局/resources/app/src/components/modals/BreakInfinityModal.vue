<script>
import ModalWrapperChoice from "@/components/modals/ModalWrapperChoice";

export default {
  name: "BreakInfinityModal",
  components: {
    ModalWrapperChoice
  },
  computed: {
    message() {
      const infinity = formatPostBreak(Number.MAX_VALUE, 2);
      return `打破无限将允许你获得超过 ${infinity} 的反物质${PlayerProgress.eternityUnlocked()
        ? "." : `，并允许你读取超过 ${infinity} 的数字。`}
        维度和 Tickspeed 升级在反物质超过 ${infinity} 后成本增长更快。
        你将根据超过 ${infinity} 的反物质产量在大坍缩时获得额外的无限点数。\
        ${EternityMilestone.keepAutobuyers.isReached ? "" : `\n它还将解锁打破无限升级，并将\
        所有普通挑战自动购买器升至上限。`}`.split("\n");
    },
  },
  methods: {
    handleYesClick() {
      breakInfinity();
    }
  },
};
</script>

<template>
  <ModalWrapperChoice
    :show-cancel="false"
    @confirm="handleYesClick"
  >
    <template #header>
      你正在打破无限
    </template>
    <div class="c-modal-message__text">
      <span
        v-for="(line, index) in message"
        :key="index"
      >
        {{ line }} <br>
      </span>
    </div>
    <template #confirm-text>
      打破
    </template>
  </ModalWrapperChoice>
</template>
